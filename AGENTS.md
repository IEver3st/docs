# Cortex documentation

This is a Mintlify MDX site. `docs.json` owns navigation; inspect it before structural changes. Keep page title/description frontmatter, valid component syntax, working example commands, and targets that exist. Check inbound links before removing or renaming a page.

Write concise, direct instructions using current product behavior. Prefer existing Mintlify components and page conventions over custom HTML. Keep code examples realistic and preserve exact identifiers.

Run `mint broken-links` for navigation or link changes. For layout/component changes, use `mint dev` and inspect affected pages only when a hidden preview or safe secondary-monitor session is available. Follow global monitor restrictions; a routine wording edit does not require launching a browser.
